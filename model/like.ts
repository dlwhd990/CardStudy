type Like = {
  _id: object;
  folderId: string;
  title: string;
  like: number;
  likedUserId: string;
  likedUserName: string;
  authorId: string;
  authorName: string;
  date: number;
};

export default Like;
