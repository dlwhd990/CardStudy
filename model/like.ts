type Like = {
  _id: object;
  folderId: string;
  title: string;
  like: number;
  likedUserId: string;
  likedUserName: string;
  authorId: string;
  authorName: string;
  problemCount: number;
  date: number;
};

export default Like;
